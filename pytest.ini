[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    --strict-markers
    --strict-config
# Home Assistant's own test harness treats warnings as errors in places; keep
# the default here so a new deprecation shows up without failing the build.
filterwarnings =
    default
