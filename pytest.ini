[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    --strict-markers
    --strict-config
# Default warning filter: a new deprecation shows without failing the build.
filterwarnings =
    default
